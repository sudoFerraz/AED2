#include "arvores.h"
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[]) {
//Cria a raiz da arvorebin e checa se o ponteiro esta vazio
arvorebin *x;

int i = 0;

while (i != 0) {

switch (i) {

x = cria_arvorebin(*x)

printf("Digite um numero para escolher a opcao\n");
//Escaneando um numero para criar a arvore
printf("1.Cria uma raiz vazia \n", );
printf("2.Checa se vazio\n", );
printf("3. Deleta a arvore\n", );
printf("4. Insere um nodo folha ou raiz\n", );
printf("5. Insere um nodo folha\n")
printf("6. Deleta um nodo na arvore\n")
printf("7. Cria uma sub-arvore com os elementos desejados separados por virgula \n", );

scanf("%d\n", );
  case 1:
    x = cria_arvorebin;
    break
  case 2:
    if(checasevazio(*raiz)){
      printf("A raiz esta vaiza\n", );
    }



}

}

  return 0;
}
